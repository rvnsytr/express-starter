meta {
  name: /api/auth/verify-email
  type: http
  seq: 2
}

get {
  url: {{api-url}}/api/auth/verify-email?token={{token}}
  body: none
  auth: none
}

params:query {
  token: {{token}}
  ~callbackUrl: 
}

vars:pre-request {
  token: 123
}

settings {
  encodeUrl: true
  timeout: 0
}
